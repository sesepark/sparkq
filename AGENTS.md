# AGENTS.md — 이 기계에서 GPU를 쓰려는 에이전트에게

이 파일은 사람이 아니라 **에이전트**(Claude Code, Codex, 그 밖의 자동화)를 위한 것이다.
README.md가 "왜 이렇게 만들었나"라면, 이 파일은 "그래서 너는 무엇을 해도 되고 무엇을 하면
안 되는가"다. 짧게 읽고 지켜라. 규칙은 하나뿐이다.

> **이 기계에서 GPU를 쓰는 것은 무엇이든 sparkq를 통해서만 띄운다.**

## 왜 하나뿐인 규칙인가

이 기계(DGX Spark, GB10)의 GPU는 하나이고 메모리는 CPU와 **통합**이다. 두 작업이 겹치면
OOM으로 한쪽이 깨끗하게 죽는 대신 **둘 다 스왑으로 느려진다.** 몇 시간짜리 학습이 두 배로
늘어나도 로그에는 아무 오류가 없다. 사람은 다음 날 아침에야 알아챈다. 큐는 이 한 가지를
막으려고 존재한다 — GPU 앞에 줄을 세우고 한 번에 하나만 돌린다.

"이건 가벼우니까 같이 돌려도 된다"는 판단을 **에이전트가 하지 않는다.** 그 판단이 맞는지는
에이전트가 알 수 없다(다른 세션이 무엇을 걸어 뒀는지, 사람이 무엇을 기다리는지 모른다).
가벼운 것은 곁다리(side lane)로 걸면 즉시 돈다. 걸어야 할 이유는 있고 안 걸 이유는 없다.

## 무엇이 "GPU를 쓰는 것"인가

전부다. 학습만이 아니다.

| 하려는 일 | 이렇게 한다 |
|---|---|
| Isaac Lab 강화학습 | `sparkq add isaac-rl task=… num_envs=… max_iterations=…` |
| LeRobot 정책 학습 | `sparkq add lerobot-train dataset=… policy=…` |
| 배운 정책을 WebRTC로 보기 | `sparkq add isaac-play task=Isaac-Lift-Cube-SO101-Play-v0` — 곁다리, 즉시 시작, 10분 뒤 자동 종료, 최신 체크포인트 자동 선택 |
| 환경 검증 스크립트, 렌더링, 측정 (2~5분짜리) | **아직 종류가 없다.** 사용자에게 종류(`kinds/isaac-check.json` 같은 것)를 추가할지 묻는다. 그 전까지는 걸 수 없다 — 큐 밖에서 띄우지 않는다 |
| Isaac Sim을 켜는 모든 파이썬 (`AppLauncher`, `isaaclab.sh -p …`) | 위와 같다. 화면 없이(headless) 켜도 GPU를 쓴다 |
| `lerobot`·`torch`로 CUDA를 건드리는 모든 파이썬 | 위와 같다 |

## 하면 안 되는 것 — 실제로 일어났던 일

2026-09-06에 한 에이전트(Claude Code)가 아래를 전부 했다. 각각 무엇이 깨졌는지도 적는다.

1. **`nohup docker exec isaac-lab-base … train.py … &` 로 학습을 직접 띄웠다.**
   → 사용자가 같은 학습을 큐에 걸었고, 큐 잡이 시작되자 직접 띄운 쪽이 죽었다. 350반복 낭비.
2. **`play.py --livestream 2` 뷰어를 직접 띄우고 켜 두었다.**
   → 그 프로세스가 GPU를 잡고 있어서 큐의 다음 잡(SmolVLA 학습)이 시작하지 못했다.
   `sparkq ls`에 "GPU를 큐 밖의 프로세스가 쓰고 있습니다"가 떴는데 그게 자기 프로세스였다.
3. **배포된 환경 설정(행동·관측 차원)을 고치는 동안** 사용자가 큐에 건 `isaac-play`가
   두 번 실패했다. 뷰어는 가장 최근 체크포인트를 자동으로 고르는데, 옛 체크포인트와 새
   설정의 텐서 크기가 맞지 않았다. **큐 밖에서 소스를 건드리면 큐가 돌리는 잡까지 깨진다.**
4. 검증 스크립트를 열댓 번 `docker exec`로 직접 돌렸다. 마침 학습이 도는 중이었고 환경이
   8개라 눈에 띄는 피해는 없었지만, 그것은 운이다.

그러니:

- `docker exec isaac-lab-base …`로 파이썬을 띄우지 않는다. 읽기(`cat`, `grep`, `ls`)는 된다.
- `nohup … &`, `tmux new -s train-…`로 GPU 프로세스를 만들지 않는다.
- 큐가 돌리는 잡을 `kill`하지 않는다. 세워야 하면 `sparkq rm <id>`. 그것도 남의 잡이면 먼저 묻는다.
- 큐에 걸린(대기 중인) 잡이 읽을 소스·설정을 바꿀 때는, 그 잡이 무엇을 읽게 될지 먼저 생각한다.
  `~/IsaacLab/source`는 컨테이너의 `/workspace/isaaclab/source`와 같은 디렉토리다.
- `kinds/`에 자유 문자열을 받는 종류를 만들지 않는다. README에 이유가 있다 — 그 순간 큐는
  원격 셸이 된다. 종류는 값을 enum·범위·이름으로만 받는다.

## 해도 되는 것

- `sparkq ls / kinds / log <id>` — 언제든.
- `sparkq add …` — 종류가 있는 것은 언제든. 순서를 바꾸고 싶으면 `top`, 빼고 싶으면 `rm`
  (자기가 건 것만).
- 로그 읽기: `~/.sparkq/runs/<id>/run.log`, `job.json`, `progress.json`. 컨테이너 안 로그는
  `docker exec isaac-lab-base cat …`으로 **읽기만**.
- 소스 편집(`~/IsaacLab/source/…`), 커밋, 문서 작성 — GPU를 안 쓰는 일은 자유다.

## 막혔을 때

"이걸 하려면 큐 밖에서 띄워야 하는데"라는 생각이 들면, 그것은 우회할 이유가 아니라 **빠진
종류**다. 사용자에게 어떤 종류가 필요한지(무엇을, 얼마나 오래, 곁다리인지) 말하고 기다린다.
기다리는 동안 GPU를 안 쓰는 일을 한다.

## 확인하는 법

자기가 규칙을 지키고 있는지 보려면:

```bash
python3 ~/sparkq/sparkq.py ls          # "GPU를 큐 밖의 프로세스가 쓰고 있습니다"가 없어야 한다
nvidia-smi --query-compute-apps=pid,process_name --format=csv,noheader
```

두 번째 출력에 큐 잡의 프로세스 외에 자기가 띄운 것이 있으면 규칙을 어긴 것이다.
